# 02 教育客服-数据中台部署（WSL Ubuntu + Docker）

本篇说明如何把教育业务数据中台（MySQL + edu-api）部署到 WSL Ubuntu 的 Docker 中，并与 Windows 本地运行的 AI 客服后端、演示前端联通。

## 1. 拓扑与端口规划

```
Windows 本机                          WSL Ubuntu (Docker)
┌──────────────────────────┐   ┌────────────────────────────────┐
│ 前端 Demo      :5174     │   │ edu-service-mysql  3307→3306   │
│ AI 客服后端    :18082    │───►│ edu-api            8000→8000  │
│        │                 │   │        │                       │
│        └── X-User-Id ────┼───┼────────┘                       │
│ 客服后端 ── aiomysql:3307─┼──►│ MySQL：customer_service + edu  │
└──────────────────────────┘   └────────────────────────────────┘
```

> 宿主机 3306 已被电商蓝本的 `customer-service-mysql` 占用，本项目 MySQL 映射到 **3307**，两套环境互不干扰、可同时运行。

## 2. 文件构成

| 文件 | 作用 |
|---|---|
| 根目录 `docker-compose.yaml` | 编排 mysql / edu-api / edu-tools（一次性数据生成任务）三个服务 |
| `edu-data/Dockerfile` | python:3.12-slim + uv 分层缓存构建；`uv sync --frozen --no-dev` 后启动 uvicorn |
| `mysql/initdb/001_init_customer_service.sql` | MySQL 容器首次空卷启动时自动创建 `customer_service.dialogue_states` 表 |

镜像构建走国内源：基础镜像 `docker.1ms.run`，pip/uv 走阿里 pypi 镜像。

## 3. 首次部署步骤

```bash
# 进入 WSL Ubuntu
wsl -e bash

cd "/mnt/c/Users/yu/Desktop/projects/vibe coding"

# ① 构建并后台启动 mysql + edu-api
docker compose up -d --build

# ② 生成仿真业务数据（smoke 小规模档：约 800 用户 / 900 订单）
docker compose run --rm edu-tools

# ③ 验证
docker compose ps                      # mysql、edu-api 均 Up
curl http://localhost:8000/health      # {"code":0,...,"status":"ok"}
curl "http://localhost:8000/api/v1/series?pageSize=3"   # 能返回课程列表
```

浏览器打开 `http://localhost:8000/docs` 可看到全部 52 个接口的 Swagger。

### 数据档位切换

```bash
docker compose run --rm edu-tools                                   # 默认 smoke
GEN_PROFILE=full docker compose run --rm edu-tools                  # 全量：10 万用户/8 万订单
docker compose down -v && docker compose up -d --build              # 彻底重置（含状态库）
```

`edu-tools` 会先执行 `init_db.py`（DROP 并重建 edu 库 + 全部表），再跑七层数据生成器，最后自动执行业务校验。

## 4. 启动本地服务（Windows）

```bash
# AI 客服后端
cd edu-customer-service
uv sync
uv run uvicorn edu_service.api.app:app --host 127.0.0.1 --port 18082
curl http://127.0.0.1:18082/health          # {"status":"ok"}

# 演示前端
cd ../edu-customer-frontend
npm install
npm run dev                                  # http://localhost:5174
```

前端通过 Vite 代理把 `/api/*` 转发到客服后端 18082，浏览器只需访问 5174 一个端口。

## 5. 关键配置项

### docker-compose.yaml（服务环境变量）

```yaml
edu-api:
  environment:
    DB_HOST: mysql        # Compose 内置 DNS：容器间用服务名互访
    DB_PASSWORD: hzk686868
    DB_NAME: edu
volumes:
  # 开发期热挂载源码，改代码重启容器即生效；生产请移除挂载重新 build
  - ./edu-data/app:/app/app
```

### edu-customer-service/.env（本地后端）

```ini
LLM_MODEL=kimi-k3
LLM_BASE_URL=https://api.moonshot.cn/v1
LLM_API_KEY=sk-****                        # 已内置可用的 key；备用 qwen 配置见注释
EDU_API_BASE_URL=http://127.0.0.1:8000/api/v1
DATABASE_URL=mysql+aiomysql://root:hzk686868@127.0.0.1:3307/customer_service?charset=utf8mb4
APP_HOST=127.0.0.1
APP_PORT=18082
```

## 6. 验收检查清单

1. `docker compose ps`：mysql 与 edu-api 都是 Up 状态；
2. `curl :8000/api/v1/me/student-profile -H "X-User-Id: 331"` 返回学员档案（该用户必须存在）；
3. `:18082/docs` 可打开，`/health` 正常；
4. Demo 页发「帮我查下订单 ORD0000000672」能收到真实订单数据；
5. 客服后端日志能看到每轮的 LLM 规划与 SQL（db echo=True）。

## 7. 常用命令速查

| 命令 | 作用 |
|---|---|
| `docker compose ps` | 查看容器状态 |
| `docker compose logs -f edu-api` | 跟踪中台日志 |
| `docker compose restart edu-api` | 重启中台（改代码热挂载后生效用这个） |
| `docker compose exec mysql mysql -uroot -phzk686868 edu` | 进 MySQL 命令行查业务库 |
| `docker compose exec mysql mysql -uroot -phzk686868 customer_service` | 查对话状态库 |
| `docker compose down` / `down -v` | 停止 / 停止并清空数据卷 |
| `docker compose up -d --build` | 改 Dockerfile 或依赖后重建启动 |

## 8. 测试账号与数据口径

smoke 档默认生成的演示学员：

| user_id | 特点 |
|---|---|
| **331**（推荐） | 5 笔订单（含已完成/已支付/已取消），在读「全栈开发系统班·直播 202606期」等多个班次 |
| 286 / 194 / 381 | 同样有多笔支付订单，可做多学员身份切换演示 |

查询某用户的可用订单号：

```bash
curl -H "X-User-Id: 331" "http://localhost:8000/api/v1/orders?pageSize=10"
```

课程关键词示例：全栈开发 / Python 数据分析方向可试「数据分析」「前端开发」「Linux」等系列名关键词。

## 9. 常见问题

**Q：edu-api 报 Internal Server Error？**
 edu 库还没生成数据，执行一次 `docker compose run --rm edu-tools`。

**Q：Windows 连不上 3307 / 8000？**
 WSL2 的 localhost 转发通常自动可用；若失效可在 PowerShell 执行 `wsl --shutdown` 后重开终端再 `docker compose up -d`。

**Q：LLM 401/限流？**
 更换 `.env` 中 LLM 三件套为 DashScope qwen-plus（模板已留注释行），重启后端即可。

**Q：想彻底重来？**
 `docker compose down -v && docker compose up -d --build && docker compose run --rm edu-tools`
