# 01 项目概述与项目架构

## 1. 项目定位

本项目是一套面向**在线教育行业**的智能客服系统，以电商客服项目为蓝本进行领域改造。核心理念不变：

> **用 LLM 理解用户意图，用代码执行具体动作。**

系统支撑在线教育平台的高频服务场景：课程咨询、订单查询、学习进度查询、退费申请、售后工单提交。

## 2. 技术栈总览

| 组件 | 技术 |
|---|---|
| AI 客服后端（本项目） | Python 3.12 · FastAPI + Uvicorn · LangChain(langchain-openai) · SQLAlchemy(aiomysql 异步) · httpx · Jinja2 · PyYAML |
| 教育业务数据中台（edu-data） | FastAPI + pymysql，66 张表的教育业务库 + 52 个 REST 接口 |
| 数据库 | MySQL 8.4 两库分离：`customer_service`（对话状态）与 `edu`（业务数据） |
| 前端 | Vue 3 + Vite 6 单页应用 |
| 部署 | 混合部署：中台容器化跑在 WSL Ubuntu Docker；客服后端与前端本地运行 |

## 3. 三服务隔离原则

AI 客服**不直连教育业务数据库**。它以"业务系统消费者"的身份通过 HTTP 调用 edu-data 中台接口：

- 所有业务请求自动携带 `X-User-Id` 学员身份头（学员身份由前端传入/对话状态记住）；
- 客服侧只持有自己的 `customer_service.dialogue_states` 单表（整个对话状态序列化为 JSON 存储，`INSERT ... ON DUPLICATE KEY UPDATE` 实现 upsert）；
- 两侧数据库完全独立，故障域隔离。

## 4. 三条互斥处理轨道

每条用户消息经 LLM 路由后**只走一条轨道**（多个候选时主动向用户澄清）：

```
用户消息
   │
   ▼
TurnPlanner(LLM 路由) ──► TurnPlanValidator(白名单校验) ──校验失败──► ClarifyResponder(澄清反问)
   │                              │校验通过
   ├─ task 轨道 ──────────► CommandProcessor(改状态) → FlowExecutor(YAML 流程推进)
   │                            └→ ActionRunner(真实调 edu-api / 回复模板)
   ├─ knowledge 轨道 ─────► KnowledgeHandler → Providers(API 实时数据 / FAQ / RAG)
   │                            └→ KnowledgeResponder(LLM 组织自然语言回复)
   └─ chitchat 轨道 ──────► ChitChatResponder(LLM 闲聊兜底，礼貌拉回业务)
```

### 4.1 任务流程轨道（Task）

面向步骤明确的业务办理。**刻意不引入 LangGraph**，自研轻量流程推进器，流程全部用 YAML 声明（`flow_config/user_flows.yml`），四种步骤类型：

| 步骤类型 | 作用 |
|---|---|
| `start` / `end` | 流程起止，end 自动清理流程上下文 |
| `collect` | 收集槽位：激活 `system_collect_information` 系统流程向用户追问，支持 `validated` 条件校验（如订单号必须 ORD 开头） |
| `action` | 执行动作：`action_response` 输出模板回复（支持 Jinja2 渲染槽位 + LLM 改写），或自定义 action 真实调用 edu-api 并回填槽位 |

内置**系统流程**（`flow_config/system_flows.yml`）：任务开启过场白、中断告知、恢复告知、取消确认、无法处理兜底等，与业务流程可交替推进。

新增一个业务流程 = 在 YAML 里追加一段配置，无需修改任何核心代码。

### 4.2 信息检索轨道（Knowledge）

即问即答类问题按意图切换答案来源（`knowledge/intents.py` 定义 7 个教育知识意图）：

| 意图 | 来源 |
|---|---|
| `order_info` 订单信息（需先发送订单卡片） | **api.order**：实时调 edu-api |
| `course_info` 课程信息（需先发送课程卡片） | **api.course**：实时调 edu-api |
| `refund_policy` 退费政策 / `open_course_policy` 开课调班政策 / `learning_service_policy` 学习服务 / `platform_rule` 平台规则 / `general_edu_info` 通用咨询 | faq.default / rag.default（**占位实现**，返回"未检索到相关信息"，与蓝本一致，后续可接入向量检索） |

Provider 注册中心模式（`knowledge/provider/`）：新增一个知识来源 = 新增一个 Provider 类并注册。

### 4.3 闲聊兜底轨道（Chitchat)

带最近 10 轮对话历史的 LLM 自由应答，保持自然的助手人设，并在合适时机把话题引回业务场景。

## 5. 架构分层（自上而下）

```
API 层        api/            chat / chat/stream(SSE) / sessions 会话管理路由 + 数据模型转换
──────────────────────────────────────────────────────────────
Service 层    services/       load_state → engine.handle_message → save_state（I/O 全在边缘）
──────────────────────────────────────────────────────────────
Engine 层     engines/        session 过期管理、消息分流、三轨道调度分发
──────────────────────────────────────────────────────────────
Plan 层       plan/           LLM 本轮规划(TurnPlan) + 白名单校验器 + 9 种澄清原因
──────────────────────────────────────────────────────────────
Task 轨道     task/           命令处理器 / 流程推进器 / 动作注册中心 / 业务动作实现
Knowledge     knowledge/      意图定义 / Provider 抽象与实现 / 响应器
Chitchat      chitchat/       闲聊响应器
Clarify       clarify/        澄清响应器（按 ClarifyReason 定制话术再 LLM 改写）
──────────────────────────────────────────────────────────────
支撑层        domain/         DialogueState 聚合根（任务上下文/暂停栈/会话轮次/卡片焦点）
              repository/     dialogue_states 单表 JSON upsert 持久化
              infrastructure/ llm_client / db_client / http_client
              prompt/         jinja2 提示词模板（turn_plan / 各轨道 respond）
              chat_history/   对话历史构建（文本消息 / 对象消息渲染）
```

## 6. 五大设计要点

1. **三轨道分离**：任务、信息、闲聊互斥执行，边界清晰。
2. **LLM 只做路由，不做决策落地**：LLM 输出 TurnPlan 后经过白名单校验（命令合法性 / 流程存在性 / 单流程约束），失败一律进入澄清兜底，不裸信 LLM 输出。
3. **YAML 定义流程，代码推进流程**：加流程改配置，加动作写 Action 类自动注册（`task/action/builder.py` 包扫描自动发现）。
4. **Service 管 I/O，Engine 纯计算**：状态读写集中在 Service 边缘，引擎内部不碰数据库（DDD 风格），天然支持流式与异步扩展。
5. **学员身份贯穿始终**：对话请求携带 `user_id` → 存入 DialogueState → 所有 edu-api 调用以该身份执行；同一会话内订单号、班次等上下文持续被 LLM 复用（如查完订单说"我要退费"，订单号槽位自动带入）。

## 7. 对象消息（卡片）特殊通道

学员点击页面上的订单卡 / 课程卡 / 班次卡时，对象消息**不经 LLM 规划**，直接进任务轨道：

| 卡片类型 | 映射槽位 |
|---|---|
| order 订单卡 | `order_number = 卡片id（订单号）` |
| course 课程卡 | `course_keyword = 卡片标题` |
| cohort 班次卡 | `cohort_name = 卡片标题` |

若当前 collect 步骤正好缺这个槽位则自动填充推进；否则落到澄清器给出去向建议（查状态 / 退费 / 工单…）。

## 8. 与电商蓝本的差异对照

| 维度 | 电商蓝本 (ecommerce) | 本项目 (edu-customer-service) |
|---|---|---|
| 业务后端 | ecommerce-service-backend（9 个接口） | edu-data（52 个接口 + 数据生成器） |
| 业务流程 | 查订单/物流/退款/相似推荐/人工 | 课程咨询/订单查询/学习进度/退费申请/工单提交/人工 |
| 对象消息 | 商品卡、订单卡 | 课程卡、班次卡、订单卡 |
| 知识意图 | 7 个电商意图 | 7 个教育意图（政策类名称对应教育场景） |
| 学员身份 | 无（接口匿名） | X-User-Id 贯穿（edu-api 全部接口要求） |
| 订单号适配 | 接口直接按单号查询 | 中台以数字 ID 驱动，action 层封装「订单号→ID」反查 |
| API 能力 | 仅非流式 /api/chat | 非流式 + SSE 流式 + 会话管理接口组（建会话/历史/状态） |
| RAG/FAQ | TODO 占位 | 同样占位（范围决策，聚焦任务闭环验收） |

## 9. 目录结构

```
edu-customer-service/
├── .env                          # LLM / 中台地址 / 状态库连接
├── pyproject.toml                # uv 管理依赖
├── flow_config/
│   ├── user_flows.yml            # 6 个业务流程 + 14 个槽位定义
│   └── system_flows.yml          # 7 个系统流程
├── scripts/
│   └── smoke_dialogue.py         # 场景化冒烟脚本（含 SSE 演示）
└── edu_service/
    ├── main.py                   # 启动入口（18082）
    ├── api/                      # app / chat_router(含SSE) / dependencies / schemas
    ├── services/dialogue_service.py
    ├── engines/                  # dialogue_engine / builder
    ├── plan/                     # planner / turn_plan / validator
    ├── task/
    │   ├── handler.py
    │   ├── commands/             # start_flow / set_slots / resume_flow / cancel_flow
    │   ├── flows/                # executor(双循环推进器) / steps / links / loader
    │   └── action/
    │       ├── builtin/          # action_listen / action_response
    │       └── customer/         # shared(edu-api客户端) + 5 个业务动作
    ├── knowledge/                # intents / handler / responder / provider/
    ├── chitchat/ clarify/ domain/ repository/ infrastructure/ prompt/ chat_history/
    └── config/settings.py        # pydantic-settings 读 .env
```
